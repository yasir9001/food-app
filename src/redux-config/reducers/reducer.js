const initState = {
    
}

export default (state=initState, action) => {
    switch(action.type) {
        case "ANYCASE":
            return state;
        default:
            return state;
    }
}